// ─── Loop Storage ───────────────────────────────────────────────
// SQLite-backed persistence for loop specs, runs, and passes.
// Migration 041 creates loop_specs, loop_runs, loop_passes tables.

export { LoopStorage } from './loop-storage.js';
